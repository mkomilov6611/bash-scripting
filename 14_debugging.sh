#!/bin/bash

# set -x Prints commands and their arguments as they executed
# set -v Prints shell input lines as they read
# set -e Exit immediately if a command exits with a non-zero status

# more debugging commands can be found here -> `help set`
